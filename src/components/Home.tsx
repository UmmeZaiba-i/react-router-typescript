// import {posts} from '../types';

const Home: React.FC = () => {
  return (
    <div>
      <h4>Home Page</h4>
      {/* {posts.map((post)=>(
        <div key={post.id}>
            <h4>{post.title}</h4>
            <h4>{post.content}</h4>
                <p> {post.author.name} {post.author.email}</p>
        </div>
      ))} */}
    </div>
  )
}

export default Home

